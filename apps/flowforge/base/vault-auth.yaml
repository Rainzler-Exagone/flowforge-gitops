apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth

metadata:
  name: flowforge-vault-auth
  namespace: flowforge

spec:
  method: kubernetes
  mount: kubernetes

  vaultConnectionRef: vault

  kubernetes:
    role: flowforge
    serviceAccount: default